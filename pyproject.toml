[project]
name = "shareable-updater"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx==0.27.2",
    "pydantic>=2.13.4",
]
